import {useEffect,useState} from 'react';import API from '../services/api';export default function Orders(){const[orders,setOrders]=useState([]);useEffect(()=>{API.get('/orders').then(r=>setOrders(r.data))},[]);return <section className="section"><h2>Order history</h2>{orders.map(o=><div className="order" key={o.id}><div><b>Order #{o.id}</b><small>{new Date(o.createdAt).toLocaleString()}</small></div><span className="tag">{o.status}</span><strong>₹{o.total}</strong></div>)}{!orders.length&&<div className="empty">No orders yet.</div>}</section>}
