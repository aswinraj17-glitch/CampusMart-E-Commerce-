import {useEffect,useState} from 'react';import API from '../services/api';import ProductCard from '../components/ProductCard';export default function Wishlist(){const[items,setItems]=useState([]);const load=()=>API.get('/wishlist').then(r=>setItems(r.data));useEffect(load,[]);return <section className="section"><div className="section-head"><h2>Wishlist</h2></div><div className="grid">{items.map(i=><ProductCard key={i.id} p={i.product} onChange={load}/>)}</div>{!items.length&&<div className="empty">No saved products yet.</div>}</section>}
