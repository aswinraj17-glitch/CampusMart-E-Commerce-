import {createContext,useContext,useState} from 'react';const C=createContext(null);export function AuthProvider({children}){const[user,setUser]=useState(()=>JSON.parse(localStorage.getItem('campusmart_user')||'null'));const login=u=>{localStorage.setItem('campusmart_token',u.token);localStorage.setItem('campusmart_user',JSON.stringify(u));setUser(u)};const logout=()=>{localStorage.removeItem('campusmart_token');localStorage.removeItem('campusmart_user');setUser(null)};return <C.Provider value={{user,login,logout}}>{children}</C.Provider>}export const useAuth=()=>useContext(C);
