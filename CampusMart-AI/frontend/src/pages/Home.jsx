import {useEffect,useState} from 'react';import {Search,Sparkles,ArrowRight} from 'lucide-react';import API from '../services/api';import ProductCard from '../components/ProductCard';import {Link} from 'react-router-dom';export default function Home(){const[products,setProducts]=useState([]),[q,setQ]=useState(''),[ai,setAi]=useState(''),[answer,setAnswer]=useState(''),[loading,setLoading]=useState(false);const load=async()=>{const r=await API.get('/products',{params:q?{q}:undefined});setProducts(r.data)};useEffect(()=>{load()},[]);const recommend=async()=>{if(!ai.trim())return;setLoading(true);setAnswer('');try{const r=await API.post('/ai/recommend',{requirement:ai});setAnswer(r.data.message)}catch(e){setAnswer(e.response?.data?.error||'Start LM Studio with Gemma 2B to use AI recommendations.')}finally{setLoading(false)}};return <main><section className="hero"><div><div className="eyebrow"><Sparkles size={15}/> STUDENT MARKETPLACE</div><h1>Buy smarter.<br/><span>Sell campus-ready.</span></h1><p>One trusted marketplace for college essentials — buy, sell, exchange or donate.</p><div className="search"><Search/><input value={q} onChange={e=>setQ(e.target.value)} onKeyDown={e=>e.key==='Enter'&&load()} placeholder="Search calculators, books, electronics..."/><button onClick={load}>Search</button></div></div><div className="hero-card"><div className="orb">🤖</div><h3>CampusMart AI</h3><p>Tell us what you need. AI recommends from real available listings.</p></div></section><section className="section"><div className="section-head"><div><span className="eyebrow">DISCOVER</span><h2>Featured essentials</h2></div><Link to="/sell" className="text-link">List a product <ArrowRight size={16}/></Link></div><div className="grid">{products.map(p=><ProductCard key={p.id} p={p}/>)}</div>{!products.length&&<div className="empty">No products found. Try another search.</div>}</section><section className="ai-box"><div><span className="eyebrow"><Sparkles size={15}/> AI ASSISTANT</span><h2>Find the right product faster</h2><p>Recommendations are generated by your local LM Studio Gemma 2B model using product data selected by the Java backend.</p></div><div className="ai-form"><textarea value={ai} onChange={e=>setAi(e.target.value)} placeholder="Example: I need a scientific calculator under ₹500 for engineering"/><button className="btn" onClick={recommend} disabled={loading}>{loading?'Thinking...':'Get AI Recommendation'}</button>{answer&&<div className="ai-answer">{answer}</div>}</div></section></main>}
